[build]
  base    = "."
  command = "npm run build"
  publish = "dist"

# SPA fallback — send all routes to index.html so React Router works
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
